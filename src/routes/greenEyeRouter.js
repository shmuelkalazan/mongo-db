const router = require('express').Router()

router.post('/',()=>{})

router.get('/my',()=>{})

router.get('/:id',()=>{})

router.post('/reply',()=>{})

router.delete('/:id',()=>{})

module.exports = router;